# Email Workflow Manager Configuration

# Storage directory for tokens and config
# Default: ~/.email-workflow
STORAGE_DIR=~/.email-workflow

# Rate limiting delay (milliseconds)
# Default: 250ms between operations
RATE_LIMIT_DELAY=250

# Max emails to process in one batch
# Default: 100
MAX_BATCH_SIZE=100

# Enable debug logging
DEBUG=false
